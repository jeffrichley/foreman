"""Packaged templates loaded via :mod:`importlib.resources`.

Currently holds the ``.foreman/INSTRUCTIONS.md`` template written by
``foreman init``. Future templates (project README stubs, label
descriptions, etc.) go here so the loaders in
:mod:`foreman.init` have a single canonical resource root.
"""
